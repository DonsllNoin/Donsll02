<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.donsll02">
    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 手机内部资源访问权限 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Donsll02">
        <activity android:name=".LitePal.LitePalActivity"></activity>
        <activity android:name=".Retrofit.RetrofitActivity"></activity>
        <activity android:name=".OkHttp.OkHttpActivity" />
        <activity android:name=".Glide.GlideActivity" />
        <activity android:name=".Wechat.MainWechatActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewPager.ViewPagerActivity" />
        <activity android:name=".Fragment.MainFragmentActivity2" />
        <activity android:name=".Fragment.MainFragmentActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>